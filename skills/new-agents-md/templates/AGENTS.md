# Project agent guidance

<!-- Keep only sections supported by repository evidence. Delete comments and unused headings. -->

## Project orientation

- State the project purpose and only the architecture boundaries an agent cannot infer from the file tree.

## Setup and commands

- Install dependencies: `<verified command>`.
- Run the focused check for `<area>` from `<directory>`: `<verified command>`.
- Run the required repository check before handoff: `<verified command>`.

## Conventions

- Use `<project-specific pattern>` because `<brief reason or safe alternative>`.
- Follow `<linked guide>` when changing `<scope>`.

## Boundaries

- Do not edit `<generated or protected path>`; instead `<safe source and regeneration command>`.
- Treat `<data or security constraint>` as required when `<condition>`.

## Completion

- For `<scope>`, verify `<concrete test or check>` and report `<required evidence>`.
