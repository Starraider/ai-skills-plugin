# <Action-oriented workflow name>

**Purpose:** <repeated outcome and intended audience>

**Run when:** <manual command, schedule, or documented trigger>

**Inputs:** <required parameters, source material, and validation rules>

**Authority:** <read-only by default; exact approved writes/tools, if any>

## Procedure

1. Confirm <precondition> and stop with <message> if it is not met.
2. Inspect <named evidence/source of truth>.
3. Produce or perform <bounded action>, keeping <variable input> parameterized.
4. Verify <observable success condition>.
5. Return <deliverable format>, including evidence, uncertainties, and next action.

## Approval and stop conditions

- Ask before <external write, deployment, publishing, sending, or deletion>.
- Stop and escalate if <missing data, policy conflict, ambiguous target, or failed verification>.
- Never include credentials or secret values in the output.
